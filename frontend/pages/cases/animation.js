import $ from 'jquery';
import { TimelineMax, Power2 } from 'gsap';

export function CasesEnterAnimation() {
  return new Promise((resolve, reject) => {
    const tl = new TimelineMax();

    // const cases = $('.cases');

    // tl.from(cases, 1, { autoAlpha: 0, yPercent: '10%', ease: Power2.easeOut }, 0);
  })
}
